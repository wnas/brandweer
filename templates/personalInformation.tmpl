{{#information}}
    <div class="information">
        <h2>{{personalInformation.header}}<button class="close">close</button></h2>
        <div>{{{personalInformation.body}}}</div>
    </div>
{{/information}}
<h2>hedaer tekstek</h2>
{{#personalInformation}}
    <div class="f-container {{#if required}}{{required}}{{/if}}">
        <label
                for="contact-{{name}}"
                class="f-label">{{name}}</label>
        <input
                type="{{type}}"
                id="contact-{{name}}"
                value="{{value}}"
                placeholder="{{placeholder}}"
                class="f-input"/>
    </div>
{{/personalInformation}}