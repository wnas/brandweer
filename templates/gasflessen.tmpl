{{#information}}
    <div class="information">
        <h2>{{gasflessen.header}}<button class="hideInformation">close</button></h2>
        <div>{{{gasflessen.body}}}</div>
        <div class="image"><img src="{{gasflessen.image}}" ></div>
    </div>
{{/information}}

{{#gasflessen}}

    <div class="copy">
            <h2>{{copy.header}}</h2>
            <div>{{{copy.body}}}</div>
    </div>
{{/gasflessen}}