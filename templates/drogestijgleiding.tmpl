{{#information}}
    <div class="information">
        <h2>{{drogestijgleiding.header}}<button class="hideInformation"><span>close</span></button></h2>
        <div>{{{drogestijgleiding.body}}}</div>
    </div>
{{/information}}

{{#drogestijgleiding}}
    <h2 class="main">{{header}}</h2>
    {{{body}}}
{{/drogestijgleiding}}