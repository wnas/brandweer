{{#information}}
    <div class="information">
        <h2>{{drogestijgleiding.header}}<button class="hideInformation">close</button></h2>
        <div>{{{drogestijgleiding.body}}}</div>
    </div>
{{/information}}

{{#drogestijgleiding}}
    <h2>{{header}}</h2>
    {{{body}}}
{{/drogestijgleiding}}